language: csharp
compiler:
    gcc
    clang

before_install:
    - sudo apt-get install libevent-dev
    - cmake --version
    - clang --version
    - gcc --version

script:
    - echo $CC $CXX
    - ./autogen.sh --prefix=/usr
    - make
    - sudo make install

